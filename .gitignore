# macOS
.DS_Store
.AppleDouble
.LSOverride

# Linux
*~
.directory

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# D1 backups (./per-ankh backup) — .sql/.sqlite dumps. Contain user emails and
# online_id PII; never commit.
backups/

# Node.js / Frontend
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# Svelte / SvelteKit
/build
/.svelte-kit
/package
.svelte-kit/
dist/

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Wrangler local state — written when adapter-cloudflare's build/dev paths
# invoke wrangler from the repo root. Cloud Worker state lives under
# cloud/.wrangler (already ignored by cloud/.gitignore).
.wrangler/

# Environment Variables
.env
.env.*
!.env.development
!.env.example
# Cloudflare Access service-token credentials for staging smoke probes
# (CF_ACCESS_CLIENT_ID / CF_ACCESS_CLIENT_SECRET) — see CLAUDE.md Deploy CLI.
.staging.vars

# Bake outputs — derived from a local pinacotheca checkout via `npm run bake:all`.
# These are visual assets owned by Mohawk Games (see LICENSE "Third-Party
# Content"). They're regenerated locally rather than tracked in git.
static/atlases/
static/sprites/
assets/atlas-sources/

# Bake-pipeline JSON sidecars. Each individual bake registers its hashed
# outputs here; scripts/build-manifests.ts reads them to emit the committed
# TS modules at src/lib/generated/{atlas,sprite}-manifest.ts.
/.bake/

# DuckDB Database Files
*.db
*.db.wal
*.duckdb
*.duckdb.wal

# ELO calculator historical match snapshots (scripts/elo-calculator.py
# --source / export snapshot) — fetched or scraped match data, kept local and
# regenerated rather than tracked in git, same reasoning as backups/ and
# Reference above. See scripts/data/README.md.
scripts/data/*
!scripts/data/.gitkeep
!scripts/data/README.md

# Test Data / Save Files
test-data/saves/*
!test-data/saves/.gitkeep
!test-data/saves/sample.xml
!test-data/saves/README.md

# Game asset reference checkout (XML, source, graphics) — read by
# scripts/bake-improvements.ts at bake time. Local-only; not committed.
# Bare name (not Reference/) so it also matches when Reference is a symlink
# into the Steam install directory.
Reference

# Python (from old experiments/demos)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/
*.egg-info/

# IDEs
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
*~
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test Coverage
coverage/
.nyc_output/

# Temporary files
*.tmp
*.temp
.cache/
